# Rust
/target
/dist
**/*.rs.bk
Cargo.lock.bak

# Extension build output (regenerated by `make extension`)
clients/extension/wasm/

# IDE
.idea/
.vscode/
*.iml
.claude/

# Web vault build output (regenerated by `make web`)
clients/web/dist/
clients/web/node_modules/
clients/web/public/wasm/

# Env / local
.env
.env.local
*.local

# Secrets — defense in depth (none of these exist today; this prevents
# accidental future commits)
*.pem
*.key
*.pfx
*.p12
credentials.json
credentials*.json
id_rsa*
id_ed25519*
id_ecdsa*
*.kdbx

# Data / runtime
/data
*.sqlite
*.sqlite-*
*.db

# OS
.DS_Store
Thumbs.db

# Docker
.docker-cache/
