{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window permissions for the main Hekate window. No plugin or filesystem access is granted. The app exposes four custom Touch ID commands (biometric_available/enable/unlock/disable) via invoke_handler — app commands are allowed by default to the app's own windows in Tauri 2; the biometric gate + key custody live in the native Swift helper (see docs/desktop-touch-id.md).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
